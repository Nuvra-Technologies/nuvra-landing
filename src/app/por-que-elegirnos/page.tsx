import { createMetadata } from "@/lib/seo";

export const metadata = createMetadata({
    title: "Por qué elegirnos | Nuvra",
    description:
        "Descubrí por qué nuestros clientes nos eligen: enfoque estratégico, calidad técnica y resultados reales.",
    path: "/por-que-elegirnos"
});

export default function PorQueElegirnosPage() {
    return (
        <section>
            {/* Por qué elegirnos */}
        </section>
    );
}
